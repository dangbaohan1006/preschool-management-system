{
  "name": "edge-api",
  "version": "1.0.0",
  "description": "Preschool Attendance Edge API Buffer",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "d1:init": "wrangler d1 create preschool-buffer",
    "d1:migrate": "wrangler d1 execute preschool-buffer --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^3.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "vitest": "^4.1.4",
    "wrangler": "^4.79.0"
  }
}
